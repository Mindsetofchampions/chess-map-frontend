# Copy to .env.scripts.local to run smoke/sanity scripts and migration pushes

# Supabase DB for migration scripts
# Option A (recommended): Full pooled connection string from Supabase → Settings → Database
# Example:
# SUPABASE_DB_URL=postgresql://postgres.<project-ref>:[YOUR-PASSWORD]@aws-0-<region>.pooler.supabase.com:6543/postgres?sslmode=require

# Option B: Provide only the pooled password and keep the URL template in supabase/.temp/pooler-url
# File supabase/.temp/pooler-url should contain (with placeholder):
#   postgresql://postgres.<project-ref>:[YOUR-PASSWORD]@aws-0-<region>.pooler.supabase.com:6543/postgres
# Then set:
# SUPABASE_DB_PASSWORD=your-pooled-password

# Supabase project (used by scripts/smoke tests, not required for migrations)
SUPABASE_URL=
SUPABASE_ANON_KEY=
# For auto-provisioning a temporary org_admin if ORG creds not provided
# SUPABASE_SERVICE_ROLE_KEY=

# Org (staff or org_admin) account used to create a quest
ORG_EMAIL=
ORG_PASSWORD=

# Optional: master admin to approve quest
# MASTER_EMAIL=
# MASTER_PASSWORD=

# Optional: force attribute id to use for quest creation
# SMOKE_ATTRIBUTE_ID=
